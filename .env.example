# ── Required ─────────────────────────────────────────────────────────────────
# Google AI Studio API key — required for RouteZero AI Co-Pilot
# Get yours free at: https://aistudio.google.com
GEMINI_API_KEY=your_gemini_api_key_here

# ── Optional: Runtime Configuration ──────────────────────────────────────────
# Set to 1 to use pre-recorded demo data (no live Pathway pipeline needed)
DEMO_MODE=0

# Override Pathway shared volume path (for Docker deployments)
TMP_DIR=./tmp

# Logging verbosity: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# ── Optional: API Configuration ───────────────────────────────────────────────
# FastAPI server host/port (default: localhost:8000)
API_HOST=0.0.0.0
API_PORT=8000

# ── Optional: Streaming Pipeline ─────────────────────────────────────────────
# Telemetry event interval in seconds (default: 2.0)
TELEMETRY_INTERVAL_SEC=2.0

# ── Frontend ──────────────────────────────────────────────────────────────────
# Override FastAPI endpoint for remote deployments
# NEXT_PUBLIC_API_URL=http://localhost:8000

# ── Mapbox ────────────────────────────────────────────────────────────────────
# Required for real-time map (free at mapbox.com, 50k loads/month)
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_mapbox_public_token_here
